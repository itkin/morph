%div[user]

  %div
    %label= t :name, :scope => [:activerecord, :attributes, :user], :default => 'name'
    %span= user.name
  %div
    %label= t :email, :scope => [:activerecord, :attributes, :user], :default => 'email'
    %span= user.email
